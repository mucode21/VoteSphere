[workspace]
resolver = "2"
members = [
    "election-registry",
    "voting",
    "results"
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1
